<Page
    x:Class="FastMD.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FastMD"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{StaticResource settings}"
    SizeChanged="Page_SizeChanged">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <SplitView x:Name="MainSplitView" DisplayMode="Inline" PaneBackground="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"  IsPaneOpen="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="48" x:Name="TopRow"/>
                    <RowDefinition x:Name="MidRow"/>
                    <RowDefinition Height="Auto" x:Name="BtmRow"/>
                </Grid.RowDefinitions>
                <Grid Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <AppBarSeparator/>
                        <!--<controls:TextToolbar Editor="{Binding ElementName=MDInputArea}" Format="MarkDown" UseUriChecker="True"  HorizontalAlignment="Center" x:Name="TextToolBar"/>-->
                        <AppBarSeparator/>
                    </StackPanel>
                    <CommandBar OverflowButtonVisibility="Collapsed" DefaultLabelPosition="Collapsed" HorizontalAlignment="Left">
                        <AppBarButton Icon="More" x:Name="PaneButton" Click="PaneButton_Click"/>
                    </CommandBar>
                </Grid>
                <Button Grid.Row="2" Content="Change Orientation" Click="Button_Click"/>

                <StackPanel Grid.Row="1" Orientation="Vertical" x:Name="vswg" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <RichEditBox x:Name="MDInputArea" TextChanged="MDInputArea_TextChanged" VariableSizedWrapGrid.ColumnSpan="2" VariableSizedWrapGrid.RowSpan="1"/>
                    <RichEditBox x:Name="TestItem" VariableSizedWrapGrid.ColumnSpan="2" VariableSizedWrapGrid.RowSpan="1"/>
                    <!--<controls:MarkdownTextBlock x:Name="Preview"/>-->
                </StackPanel>
            </Grid>
        </SplitView>
    </Grid>
</Page>
